# KN07: Kubernetes II

## A) Begriffe und Konzepte

### Pods vs. Replicas

Ein Pod ist die kleinste Einheit in Kubernetes und enthält einen oder mehrere Container. Eine Replica ist eine Kopie eines Pods. Replicas sorgen dafür, dass mehrere identische Pods gleichzeitig laufen — für Ausfallsicherheit und Lastverteilung.

### Service vs. Deployment

Ein Deployment definiert, wie viele Pods laufen sollen und welches Container-Image verwendet wird. Ein Service stellt eine stabile Netzwerkadresse bereit, damit andere Pods oder externe Nutzer auf die Pods zugreifen können — unabhängig davon, welcher Pod gerade läuft.

### Ingress

Ingress löst das Problem, dass man viele Services von aussen erreichbar machen will, ohne für jeden einen eigenen LoadBalancer zu brauchen. Ingress ist ein zentraler Einstiegspunkt, der den Traffic anhand von URLs oder Hostnamen an die richtigen Services weiterleitet.

### StatefulSet

Ein StatefulSet ist für Anwendungen gedacht, die einen stabilen, persistenten Zustand brauchen. Jeder Pod bekommt eine feste Identität und eigenen Speicher. Ein Beispiel ist ein Message Broker wie Kafka, wo jeder Node seine eigene Partition behält.

---

## B) Demo Projekt

### Übersicht

In diesem Teil wird ein Demo-Projekt auf dem Kubernetes-Cluster aus KN06 deployed. Es besteht aus einer MongoDB-Datenbank und einer Node.js-WebApp, die über einen internen Service miteinander kommunizieren.

### YAML-Files

#### `mongo-config.yaml`

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: mongo-config
data:
  mongo-url: mongo-service
```

#### `mongo-secret.yaml`

```yaml
apiVersion: v1
kind: Secret
metadata:
  name: mongo-secret
type: Opaque
data:
  mongo-user: bW9uZ291c2Vy
  mongo-password: bW9uZ29wYXNzd29yZA==
```

#### `mongo.yaml`

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongo-deployment
  labels:
    app: mongo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mongo
  template:
    metadata:
      labels:
        app: mongo
    spec:
      containers:
      - name: mongodb
        image: mongo:6
        ports:
        - containerPort: 27017
        env:
        - name: MONGO_INITDB_ROOT_USERNAME
          valueFrom:
            secretKeyRef:
              name: mongo-secret
              key: mongo-user
        - name: MONGO_INITDB_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mongo-secret
              key: mongo-password
---
apiVersion: v1
kind: Service
metadata:
  name: mongo-service
spec:
  selector:
    app: mongo
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017
```

#### `webapp.yaml`

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: webapp-deployment
  labels:
    app: webapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: webapp
  template:
    metadata:
      labels:
        app: webapp
    spec:
      containers:
      - name: webapp
        image: nanajanashia/k8s-demo-app:v1.0
        ports:
        - containerPort: 3000
        env:
        - name: DB_URL
          valueFrom:
            configMapKeyRef:
              name: mongo-config
              key: mongo-url
        - name: USER_NAME
          valueFrom:
            secretKeyRef:
              name: mongo-secret
              key: mongo-user
        - name: USER_PWD
          valueFrom:
            secretKeyRef:
              name: mongo-secret
              key: mongo-password
---
apiVersion: v1
kind: Service
metadata:
  name: webapp-service
spec:
  type: NodePort
  selector:
    app: webapp
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30100
```

---

### Installation

```bash
microk8s kubectl apply -f mongo-config.yaml
microk8s kubectl apply -f mongo-secret.yaml
microk8s kubectl apply -f mongo.yaml
microk8s kubectl apply -f webapp.yaml
```

---

### Überprüfung

```bash
microk8s kubectl get all
```

<img width="1743" height="617" alt="image" src="https://github.com/user-attachments/assets/bcc280af-4da2-457c-b4dd-99320899c75f" />

---

### `microk8s kubectl describe service webapp-service` (Node 1)

```bash
microk8s kubectl describe service webapp-service
```

<img width="1969" height="755" alt="image" src="https://github.com/user-attachments/assets/09fd3bf0-1585-494b-bf16-970d9725971f" />

---

### `microk8s kubectl describe service webapp-service` (Node 2)

<img width="2061" height="392" alt="image" src="https://github.com/user-attachments/assets/8474638e-0aec-476c-8def-71d24bd800a9" />

---

### WebApp aufrufen

Die WebApp ist über den NodePort `30100` erreichbar. Die URL lautet:

```
http://13.219.47.227:30100
```
## Node 1
<img width="1874" height="1694" alt="image" src="https://github.com/user-attachments/assets/9a9c12ee-c33c-4316-a363-820b1998aa86" />
<!-- Screenshot: WebApp im Browser Node 2 -->

## Node 3
<img width="1615" height="1755" alt="image" src="https://github.com/user-attachments/assets/4bc83913-414d-4fad-8934-fbd431645b67" />

---

### Warum funktioniert MongoDB Compass nicht?

Die MongoDB ist nur über einen internen ClusterIP-Service erreichbar (`mongo-service`). Von aussen ist kein Zugriff möglich, weil kein NodePort oder LoadBalancer konfiguriert ist. Um den Zugriff zu ermöglichen, müsste man den `mongo-service` auf `type: NodePort` ändern und einen externen Port (z.B. 32000) definieren.

---

### Datenbanktyp — kein StatefulSet

Die MongoDB wurde als normales Deployment deployed, nicht als StatefulSet. Der Grund: Für dieses Demo-Projekt reicht ein einfaches Deployment aus, da keine persistente Datenspeicherung oder stabile Pod-Identitäten benötigt werden. In einer produktiven Umgebung würde man MongoDB als StatefulSet mit PersistentVolumes betreiben.

---

### Erklärung ConfigMap `mongo-url`

Der Wert `mongo-url: mongo-service` ist korrekt, weil Kubernetes intern DNS-Namen für Services erstellt. Der Name `mongo-service` entspricht dem `metadata.name` des MongoDB-Service. Die WebApp kann damit den MongoDB-Service über diesen DNS-Namen erreichen, ohne die IP-Adresse zu kennen.

---

### Port auf 32000 ändern und Replicas auf 3 erhöhen

```bash
nano webapp.yaml
```

Änderungen:
- `nodePort: 30100` → `nodePort: 32000`
- `replicas: 1` → `replicas: 3`

Dann anwenden:

```bash
microk8s kubectl apply -f webapp.yaml
```

---

### `microk8s kubectl describe service webapp-service` nach Änderung

<!-- Screenshot: describe service webapp-service nach Port-Änderung -->

---

### WebApp nach Port-Änderung

```
http://<PUBLIC-IP>:32000
```

<!-- Screenshot: WebApp im Browser auf Port 32000 -->
